import { Spo } from "./spo";

export interface ModuleManual {
    id: number | null,
    spo: Spo,
    semester: String,
   // firstPage: string | undefined,
   // generatedPDF:  string | undefined
}
